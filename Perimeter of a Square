#include <stdio.h>
int main()
{
int side, perimeter;
printf("Enter the length of one side: ");
scanf("%d", &side);
perimeter = side * 4;
printf("The perimeter of the square is: %d\n", perimeter);
return 0;
}
